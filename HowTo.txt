- git clone 'url'
- git remote -v --> pour vérification
- git --global user.email / user.name ""
Connexion :
Creation de tokens personnels dans les paramètres dévelppeurs de GitHub
On peut aussi faire grace  SSH, plus long  configurer mais plus rapide sur le long terme


Tests
Creation / modification fichier
- git add
- git commit
- git push
Si ne fonctionne pas : git merge / git pull

---
Utilisation python:

/!\ utiliser Python 3.6.9
Pour avoir le module nécéssaire : 
pip3 install pokepy ou python -m pip install pokepy
-> importer les modules nécéssaires :
import markdown
import pokepy


Doc : https://pokeapi.co/docs/v2
https://pokeapi.github.io/pokepy/usage/
Ne pas oublier le cahier des charges
